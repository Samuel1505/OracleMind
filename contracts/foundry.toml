[profile.default]
src = "src"
out = "out"
libs = ["lib"]

[rpc_endpoints]
mantle_sepolia = "https://rpc.sepolia.mantle.xyz"

[etherscan]
mantle_sepolia = { key = "${ETHERSCAN_API_KEY}", url = "https://explorer.sepolia.mantle.xyz/api" }

# Note: Chain name for verification is "mantle-sepolia" (with hyphen)